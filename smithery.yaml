# Smithery configuration file
startCommand:
  type: stdio
  configSchema:
    # JSON Schema defining the configuration options for the MCP
    type: object
    properties: {}
    required: []
  commandFunction: |
    function(config) {
      return {
        command: "node",
        args: ["dist/server.js"],
        env: {}
      };
    }